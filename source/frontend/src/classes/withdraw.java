package classes;

public class withdraw {
	
	public boolean withdrawFromAccount(frontend terminal, String account, double amount) {
		validAccounts = terminal.getvalidAccts();
		
		//if account in validAccounts
			// if sufficient ammount and not at limit
		return 0;
	}

}
